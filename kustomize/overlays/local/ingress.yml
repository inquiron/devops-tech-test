apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-tech-test-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-tech-test-service
            port:
              number: 80
