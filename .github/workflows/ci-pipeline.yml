name: CI Pipeline
on:
  push:
    branches:
      - main
      - features/*

jobs:
  trivy:
    uses: ./.github/workflows/trivy-scan.yml

  publish:
    needs: trivy
    uses: ./.github/workflows/publish.yml