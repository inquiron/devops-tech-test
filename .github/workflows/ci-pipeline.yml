name: CI Pipeline
on:
  push:
    branches:
      - main
      - feature/*

jobs:
  trivyscan:
    uses: ./.github/workflows/trivy-scan.yml

  publish:
    needs: trivy
    uses: ./.github/workflows/publish.yml